import React from 'react';
import ReactDOM from 'react-dom';
import MainApp from './App';
import './theme.css'; // Assuming you have a theme file for styles

ReactDOM.render(
      <MainApp />,
  document.getElementById('root')
);